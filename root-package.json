{
  "name": "summerberry-fullstack",
  "version": "1.0.0",
  "description": "SummerBerry Dashboard - Full Stack Development Setup",
  "scripts": {
    "dev": "concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"yellow,cyan\" \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && python -m uvicorn main:app --reload --port 8000",
    "frontend": "cd frontend && npm run dev",
    "install:all": "cd frontend && npm install",
    "setup": "echo 'Run: pip install -r backend/requirements.txt && npm run install:all'"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
